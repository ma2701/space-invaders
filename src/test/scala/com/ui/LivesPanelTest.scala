package com.ui

import org.scalatest.FunSuite


class LivesPanelTest extends FunSuite {

    test("that the lives panel can be constructed and displayed built") {
        val livesPanel = new LivesPanel
    }

 }
